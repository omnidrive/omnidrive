package omnidrive.Api.managers;

import omnidrive.Api.Base.*;

public class UploadManager {

    // will split the file into all user accounts
    public BaseFile uploadFile(AccountsManager accounts, String localFilePath) throws BaseException {
        return null;
    }

    // will create the folder in all user accounts
    public BaseFolder createRemoteFolder(AccountsManager accounts, String remoteFolderPath) throws BaseException {
        return null;
    }

}
